import React from 'react';
import styles from './NotFound.module.css'

function NotFound() {

  return (
    <h1>Page not found or you don't have permissions to visit it!</h1>
  );
};

export default NotFound;